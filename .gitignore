/android/
/ios/
node_modules/
.expo/
dist/
android/.gradle/
android/build/
android/app/build/
android/app/.cxx/
android/local.properties
*.log
.DS_Store
ios/
.agents/
# The icon generator is the source of truth for the launcher artwork: the
# PNGs it writes are committed, so the tool that produced them has to be too.
# `tools/*` rather than `tools/`: Git cannot re-include a file whose parent
# directory is excluded.
tools/*
!tools/make_icon.py
.scratch-Documents-MiraiRPG-backup/
skills-lock.json
test-android.js
firebase-debug.log
.firebase/
crash-screenshot.jpg
mirai-rpg-build-*.apk
